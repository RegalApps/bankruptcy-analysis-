
// This file is now moved to the upload directory structure
// Re-export the component from the new location

import { FileUploadSection as NewFileUploadSection } from './upload/FileUploadSection';

// Export the component with the same interface
export const FileUploadSection = NewFileUploadSection;
